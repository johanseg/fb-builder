[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[build.environment]
NIXPACKS_NODE_VERSION = "22"

[deploy]
startCommand = "sh -c 'npx serve dist -l ${PORT:-3000}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
